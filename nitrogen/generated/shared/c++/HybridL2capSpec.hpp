///
/// HybridL2capSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `Device` to properly resolve imports.
namespace margelo::nitro::l2cap { struct Device; }

#include "Device.hpp"
#include <functional>
#include <NitroModules/Promise.hpp>
#include <string>
#include <NitroModules/ArrayBuffer.hpp>

namespace margelo::nitro::l2cap {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `L2cap`
   * Inherit this class to create instances of `HybridL2capSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridL2cap: public HybridL2capSpec {
   * public:
   *   HybridL2cap(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridL2capSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridL2capSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridL2capSpec() override = default;

    public:
      // Properties
      virtual bool getIsConnected() = 0;

    public:
      // Methods
      virtual void startScan() = 0;
      virtual void stopScan() = 0;
      virtual void onDeviceFound(const std::function<void(const Device& /* device */)>& callback) = 0;
      virtual std::shared_ptr<Promise<void>> connect(const std::string& address, double psm, bool secure) = 0;
      virtual void disconnect() = 0;
      virtual void sendData(const std::shared_ptr<ArrayBuffer>& data) = 0;
      virtual void onDataReceived(const std::function<void(const std::shared_ptr<ArrayBuffer>& /* data */)>& callback) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "L2cap";
  };

} // namespace margelo::nitro::l2cap
